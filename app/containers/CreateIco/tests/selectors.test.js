// import { fromJS } from 'immutable';
// import { selectCreateIcoDomain } from '../selectors';

describe('selectCreateIcoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
