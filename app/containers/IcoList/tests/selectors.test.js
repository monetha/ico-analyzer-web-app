// import { fromJS } from 'immutable';
// import { selectIcoListDomain } from '../selectors';

describe('selectIcoListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
