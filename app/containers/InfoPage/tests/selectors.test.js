// import { fromJS } from 'immutable';
// import { selectInfoPageDomain } from '../selectors';

describe('selectInfoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
