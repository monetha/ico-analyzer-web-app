// import { fromJS } from 'immutable';
// import { selectRedirectHandlerDomain } from '../selectors';

describe('selectRedirectHandlerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
