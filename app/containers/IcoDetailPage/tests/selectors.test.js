// import { fromJS } from 'immutable';
// import { selectIcoDetailPageDomain } from '../selectors';

describe('selectIcoDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
